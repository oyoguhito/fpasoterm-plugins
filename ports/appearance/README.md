# appearance category

Appearance ports change the running terminal palette or presentation. Prefer
fpasoterm profiles for persistent window and font settings; use these ports for
runtime theme behavior.

Current ports: `amber`, `high-contrast`, and `teal`.
