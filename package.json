{
  "name": "fpasoterm-plugins",
  "version": "0.0.1",
  "private": true,
  "description": "Ports-style community plugins for fpasoterm.",
  "license": "MIT",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "check": "npm run check:syntax && npm test",
    "check:syntax": "tsc --noEmit --project tsconfig.json",
    "test": "npm run build --silent && node dist/tests.js",
    "scan:secrets": "npm run build --silent && node dist/security/scan-secrets.js",
    "audit:prod": "npm audit --omit=dev",
    "security": "npm run scan:secrets && npm run audit:prod",
    "ports": "npm run build --silent && node dist/ports.js",
    "index": "npm run ports -- index --check"
    ,"build:novnc-port": "esbuild scripts/novnc-plugin-entry.js --bundle --format=iife --target=es2020 --banner:js='// @fpasoterm-plugin version: 1.0.0\n// @fpasoterm-plugin description: Verification noVNC client for the strictly declared local TCP bridge.\n// @fpasoterm-plugin allowed-tcp-targets: tcp://127.0.0.1:59999' --outfile=ports/integration/novnc-local-bridge/plugin.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@novnc/novnc": "^1.5.0",
    "@types/node": "^26.3.0",
    "esbuild": "^0.25.12",
    "typescript": "^7.0.2"
  }
}
